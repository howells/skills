interface:
  display_name: "Foreman"
  short_description: "Plan, spec, and review; delegate the code across taste/heavy/grunt tiers"
  default_prompt: "Use $foreman to implement this: plan and spec the work, delegate the production code to subagents routed by tier and by the model you are running on, keep every brief and report terse, and review their diffs yourself."
