interface:
  display_name: "Foreman"
  short_description: "Plan, spec, and review; delegate the code"
  default_prompt: "Use $foreman to implement this: plan and spec the work, delegate the production code to subagents, and review their diffs yourself."
