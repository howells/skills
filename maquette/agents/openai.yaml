interface:
  display_name: "Maquette"
  short_description: "Compare five page directions, then build the chosen one"
  default_prompt: "Use $maquette to render this page beside five distinct design directions behind a temporary picker, ask me to choose once, then build the chosen direction and remove the rest."
policy:
  allow_implicit_invocation: false
