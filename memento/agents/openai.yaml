interface:
  display_name: "Memento"
  short_description: "Current-task status from live repository state"
  default_prompt: "Use $memento to report the current task's ask, completed work, remaining work, branch, and uncommitted files."
