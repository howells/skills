interface:
  display_name: "Plimsoll"
  short_description: "Cut the checking when it has displaced shipping"
  default_prompt: "Use $plimsoll to stop the current checking, name what the person waiting will actually look at, and cut the checks down to the one that could really fail."
