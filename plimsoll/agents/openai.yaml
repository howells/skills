interface:
  display_name: "Plimsoll"
  short_description: "Cut slow checks and keep Vercel builds local"
  default_prompt: "Use $plimsoll to stop the current checking, name what the person waiting will actually look at, keep every Vercel build on the user's machine, and cut checks to the one that could really fail."
