interface:
  display_name: "Salvage"
  short_description: "Rescue unique work before repository cleanup"
  default_prompt: "Use $salvage to rescue unique work, then remove only branches, worktrees, and stashes proven merged and pushed."
