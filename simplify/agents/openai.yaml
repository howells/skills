interface:
  display_name: "Simplify"
  short_description: "Strict review of changes or a whole codebase"
  default_prompt: "Use $simplify to apply Cursor's Thermo-Nuclear Code Quality Review rubric to the selected changes, or the whole codebase when explicitly requested, preserving its structural standards, ambitious simplification goals and approval bar. Respect the repository's instructions and existing authorization."
policy:
  allow_implicit_invocation: false
