interface:
  display_name: "Simplify"
  short_description: "Review changes or codebase health and complexity"
  default_prompt: "Use $simplify to review the selected changes, or the whole codebase when requested, using Cursor's strict structural standards and stage-aware health checks. Verify findings in source, group related fixes, state coverage, and score only if asked. Report findings unless edits or tracker updates are authorized."
policy:
  allow_implicit_invocation: false
