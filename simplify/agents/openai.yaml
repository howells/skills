interface:
  display_name: "Simplify"
  short_description: "Strict structural review of code quality"
  default_prompt: "Use $simplify to apply Cursor's Thermo-Nuclear Code Quality Review rubric to the selected changes, preserving its structural standards, ambitious simplification goals and approval bar. Respect the repository's instructions and existing authorization."
policy:
  allow_implicit_invocation: false
