interface:
  display_name: "Zoetrope"
  short_description: "Build, review or find web animation"
  default_prompt: "Use $zoetrope to decide whether this should animate at all, then build it with the right curve, duration or spring, interruption behaviour and reduced-motion handling."
